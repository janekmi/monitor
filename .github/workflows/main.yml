name: Monitor

on:
  push:

jobs:
  urls_monitor:
    name: URLs Monitor
    runs-on: ubuntu-latest
    steps:
      - name: Check an URL
        run: |
          code=$(curl -o /dev/null --silent --head --write-out '%{http_code}\n' ${{secrets.SECRET_URL}})
          [ "$code" -eq 401 ]
